import 'package:flutter/material.dart';

class MultiSelectDialogue extends StatelessWidget {
  MultiSelectDialogue({


    Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
